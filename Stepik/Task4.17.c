#include <stdio.h>
#include <math.h>

long long int ipow(long long int a, unsigned int n, unsigned int * depth);

int main (){

    long long int a = 0;
    unsigned int n = 0;
    unsigned int * depth;

    scanf("%d%d", &a, &n);

    ipow(a, n, depth);

    printf("Hello");
}

long long int ipow(long long int a, unsigned int n, unsigned int * depth) {

    

    return 0;
}